name: aura_sentinel
description: Asistente virtual inteligente de emergencias con IA offline y online
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # ===========================
  # 🎨 UI & DESIGN
  # ===========================
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  lottie: ^3.0.0
  shimmer: ^3.0.0
  flutter_spinkit: ^5.2.0
  animate_do: ^3.1.2
  
  # ===========================
  # 🔐 AUTENTICACIÓN & SEGURIDAD
  # ===========================
  # AppWrite SDK para backend
  appwrite: ^12.0.3
  
  # Biometría nativa (reemplaza GetID)
  local_auth: ^2.1.8
  flutter_secure_storage: ^9.0.0
  
  # ===========================
  # 🤖 INTELIGENCIA ARTIFICIAL
  # ===========================
  # TensorFlow Lite para IA offline (versiones compatibles)
  tflite_flutter: ^0.9.0
  image: ^4.1.3  # Para procesamiento de imágenes con IA
  
  # OpenAI API para chat conversacional
  http: ^1.1.0
  dio: ^5.4.0
  
  # ===========================
  # 🗺️ MAPAS & UBICACIÓN
  # ===========================
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  permission_handler: ^11.1.0
  
  # Mapas offline
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  
  # ===========================
  # 📞 COMUNICACIÓN & EMERGENCIAS
  # ===========================
  url_launcher: ^6.2.2
  flutter_phone_direct_caller: ^2.1.1
  contacts_service: ^0.6.3
  flutter_sms: ^2.3.3
  
  # ===========================
  # 📷 MULTIMEDIA
  # ===========================
  image_picker: ^1.0.4
  camera: ^0.10.5+7
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.5
  
  # ===========================
  # 🗄️ BASE DE DATOS LOCAL (Offline)
  # ===========================
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # ===========================
  # 🔔 NOTIFICACIONES (Solo locales - sin Firebase)
  # ===========================
  flutter_local_notifications: ^16.3.0
  
  # ===========================
  # 🎤 VOZ & AUDIO
  # ===========================
  speech_to_text: ^6.5.1
  flutter_tts: ^3.8.5
  audioplayers: ^5.2.1
  record: ^5.0.4
  
  # ===========================
  # 🛠️ UTILIDADES
  # ===========================
  # Gestión de estado
  get: ^4.6.6
  provider: ^6.1.1
  
  # Formateo y validación
  intl: ^0.18.1
  mask_text_input_formatter: ^2.7.0
  
  # Conectividad
  connectivity_plus: ^5.0.2
  internet_connection_checker: ^1.0.0+1
  
  # Manejo de archivos
  file_picker: ^6.1.1
  share_plus: ^7.2.1
  
  # Utilidades generales
  uuid: ^4.2.2
  logger: ^2.0.2+1
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^6.0.0
  build_runner: ^2.4.7
  hive_generator: ^2.0.1

# ===========================
# 📁 ASSETS
# ===========================
flutter:
  uses-material-design: true
  
  fonts:
    - family: CustomIcon
      fonts:
        

  assets:
    - assets/img/logo_welcome.png